name: midi_band
version: 0.14.0
api_version: 1
author: HoppouAI
description: Multiple Gabriel instances on the same LAN form a band, each plays different MIDI tracks of the same song in sync via a soundfont. Includes a standalone client for non-Gabriel players.
enabled: true
requirements:
  - mido>=1.3
  - pyfluidsynth>=1.3
  - google-genai>=0.3
