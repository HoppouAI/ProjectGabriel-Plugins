# Drop your .mid / .midi files into this folder and the midi_band
# plugin will pick them up. The host's `listMidiSongs` tool reads from
# here by default. Override with `library_dir:` in config to use a
# different folder instead.
