.venv/
.venv*/
__pycache__/
*.pyc
*.pyo
*.egg-info/
build/
dist/
.uv.lock.bak

# bundled uv from setup.bat
bin/

# uv lockfile, per-machine, dont ship
uv.lock

# real config (uses config.example.yml as the template)
config.yml

# model + voice cache the engine drops here at runtime
data/
ecapa_model/
voices/

# voice clips uploaded from the client (cached by sha256)
data/uploads/

# torch / hf caches if the user routes them locally
.hf_cache/
.torch_cache/

# logs
*.log
