name: omnivoice
version: 0.1.0
api_version: 2
author: HoppouAI
description: OmniVoice TTS provider (k2-fsa/OmniVoice, 600+ languages, voice cloning, streaming). Talks to a local omnivoice-serve REST endpoint.
enabled: true
requirements:
  - httpx>=0.27
  - numpy>=1.24
  - stream2sentence>=0.2
  - nltk>=3.8
